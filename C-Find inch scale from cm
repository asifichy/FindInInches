#include<stdio.h>

const float oneInchIncm = 2.54;

int main()
{
    int length;

    printf("Enter length in cm: ");
    scanf("%d", &length);

    float inch = length / oneInchIncm;

    printf("The length of a book in inch scale is: %.2f inch ", inch);

    return 0;
}
